#pragma once

// Enum liet ke 4 loai nhan vat co the chon - dung chung cho CharacterSelect,
// CombatScene va SaveManager (luu/doc save duoi dang so nguyen don gian).
enum class CharacterType {
    Warrior = 0,
    Mage = 1,
    Archer = 2,
    Tank = 3
};
